<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stories.puneet.indian.sex.stories"
    android:versionCode="4"
    android:versionName="2.3" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <!-- Adding privileges for AdVersions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
	<!--  Added this permission to ad the ADIcon to system -->
	<uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />
	
    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/Theme1" >
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StorySelectorActivity"
            android:label="StorySelector Activity" />
        <activity
            android:name=".StoryRedingActivity"
            android:label="StoryReding Activity" />
        <!-- Added this activity for video integration in the app.  -->
        <activity
            android:name=".youtube.OpenYouTubePlayerActivity"
            android:label="YouTubePlayer Activity"
            android:screenOrientation="landscape"
            android:configChanges="orientation|keyboardHidden|screenSize" 
            />

        <!-- AdMob Activity -->
        <activity
            android:name="com.google.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" />
        <!-- Mandatory activity declaration to show the INMobiAd in the embedded browser -->
        <activity android:name="com.inmobi.androidsdk.IMBrowserActivity"
            android:configChanges="keyboardHidden|orientation|keyboard|smallestScreenSize|screenSize" />

	
        <!-- This is the key for Adwhirl Ad Network. This will be helpfull in providing ad from all the networks -->
        <meta-data
            android:name="ADWHIRL_KEY"
            android:value="bf0cb7dabcef47cc864e673606054763" />
    </application>

</manifest>
